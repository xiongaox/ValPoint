1、删除共享库、后台以及登录页
2、使用sqlite替换supabase后端
3、去除图床、链接存储方式，只保留上传和剪贴
4、去除头像获取，随机下载20个，并保存到本地提供选择
5、管理员账号密码：admin && admin123
6、Edge Functions能不能在docker弄，不依赖supabase
7、sqlite存储点位图片，需要按照地图，新增文件夹，里面的文件名，也需要规范命名，如：英雄_技能_站位.webp（自动转换成webp，节省空间）
8、删除webp压缩功能，只保留格式转换，不做任何压缩
9、构建的docker包，端口号为：3209；镜像包命名：valpoint_s
10、新增弹窗，展示点位统计（每个英雄，有多少点位，总共有多少点位）